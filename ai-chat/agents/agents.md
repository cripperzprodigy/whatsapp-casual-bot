# Agent Registry and Roles

## Execution Agent
**Role**: Handles precise technical implementation and documentation updates based on strict protocols.

**Responsibilities**:
- Implementing `BotIdentityManager` and `/whatsapp/bot-identity` dynamic detection features.
- Maintaining JID normalization logic in `router_webhook.py` (e.g., `@lid`, `@c.us`, `@s.whatsapp.net`).
- Keeping ai-chat documentation synchronized after code changes.
- Executing precise patch commands and maintaining operational compliance with `ai-chat` architecture.
